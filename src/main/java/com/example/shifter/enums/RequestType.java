package com.example.shifter.enums;

/**
 * Enum for shift change request (swap, day off, etc.)
 */
public enum RequestType {
    WANT_TO_WORK,
    UNAVAILABLE,
    LEAVE_OF_ABSENCE,
    SICK_DAY,
    SHIFT_SWAP

}
